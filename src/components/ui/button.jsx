import React from 'react';export function Button({asChild,variant='default',size='base',className='',children,...props}){const base='inline-flex items-center justify-center px-4 py-2 font-semibold rounded-md shadow-sm transition hover:opacity-90';const variants={default:'bg-slate-900 text-white',secondary:'bg-gray-100 text-slate-900 border'};const sizes={base:'',lg:'text-base px-5 py-3'};const cls=[base,variants[variant]||variants.default,sizes[size]||'',className].join(' ');if(asChild&&React.isValidElement(children)){return React.cloneElement(children,{className:[children.props.className,cls].join(' ')})}return <button className={cls}{...props}>{children}</button>}